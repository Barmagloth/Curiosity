# HANDOFF — Передача проекта Curiosity

Документ для нового участника проекта (человек или AI-orchestrator). Содержит всё необходимое для продолжения работы без дополнительного контекста.

---

## Статус одной фразой

Серия из 8 экспериментов (Exp0.1–Exp0.8) завершена и валидирована. Адаптивное уточнение работает. Система готова к переходу на следующий уровень — реальные структуры данных и GPU-оптимизации.

---

## Что сделано

### Валидированные компоненты (менять нельзя без веских причин):

1. **Adaptive refinement** — selective tile refinement > random selection
2. **Halo** — cosine feathering, overlap ≥ 3 элемента, обязателен
3. **Двухстадийный гейт** — residual-first с fallback на utility-weighted combo
4. **Budget governor** — EMA-контроллер strictness, обязателен
5. **Probe** — 5–10% бюджета на exploration, обязателен
6. **SeamScore** — `Jumpout / (Jumpin + eps)`, production-ready

### Что отвергнуто:

- Phase schedule (смена весов p по шагам) — не даёт выигрыша
- Morton layout — overhead превышает выигрыш
- Block-sparse layout — то же

---

## Первая задача: Exp0.9b0

**Тип:** Buffer-scaling probe
**Приоритет:** P0 (наивысший)
**Kill criteria:** определены (см. experiment_hierarchy.md)

---

## Ключевые документы

| Документ | Содержание |
|---|---|
| `docs/concept_v1.5.md` | Каноническая концепция, все валидированные решения |
| `docs/experiment_results.md` | Подробные результаты Exp0.1–Exp0.8 с числами |
| `docs/experiment_hierarchy.md` | Граф зависимостей, приоритеты P0–P4, roadmap |
| `docs/architecture.md` | Архитектура системы, компоненты, стек |

---

## Что НЕ хватает (известные пробелы)

1. **Код** — реализации экспериментов Exp0.1–Exp0.9a. Без них нельзя воспроизвести baseline, запустить следующий эксперимент или проверить результаты.

2. **Сырые данные/результаты** — конкретные числа есть в документации, но raw data tables и логи экспериментов не сохранены в репозитории.

3. **Параметры окружения** — версии Python/PyTorch/CUDA, железо (GPU), структура директорий проекта.

4. **Форматные соглашения** — как именовать эксперименты, куда писать результаты, какой формат логов ожидается.

5. **Jupyter Notebooks** — рабочие тетрадки экспериментов не включены.

---

## Иерархия приоритетов для продолжения

```
P0: GPU layout (Exp0.9b0 — buffer-scaling probe)
P1: Tree compression (segment compression)
P2: Auto-tuning (автонастройка порогов гейта)
P3: Tree semantics (кусты, LCA-расстояние)
P4: Downstream compatibility (не сломать фичи)
```

Порядок строгий. Нельзя прыгать вперёд без закрытия зависимостей.

---

## Принципы работы

- **«Judge numbers, then ambitions»** — каждое утверждение подкреплено данными
- **Kill criteria перед запуском** — если эксперимент не проходит критерии, он закрывается
- **Cost-fair сравнения** — вычислительные затраты учтены в каждом сравнении
- **Observable-only диагностики** — никакой oracle-информации в метриках
- **Holm-Bonferroni коррекции** — при множественных сравнениях

---

## Источники проекта

Работа велась параллельно в двух средах:
- **Claude (Anthropic)** — основная документация, Memory с полным контекстом, 3 файла проекта
- **ChatGPT (OpenAI)** — дополнительные обсуждения, план работ, техзадания для кода

Этот репозиторий — консолидация данных из обоих источников по состоянию на 10 марта 2026.
